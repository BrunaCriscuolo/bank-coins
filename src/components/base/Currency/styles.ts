import { styled } from "styled-components";
import CurrencyFormat from "react-currency-format";

export const CurrencyContainer = styled(CurrencyFormat)`
  display: block;
  margin-top: 1rem;
  font-size: 2rem;
`;
